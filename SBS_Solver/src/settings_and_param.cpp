/*
#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <complex>
#include <cmath>
#include <limits>
#include <vector>
#include <chrono>
#include <algorithm>

#include "json11.hpp"

*/

//Param::Param(double In_fg = 1.45, double IGamma_B = 2 * pi*40e6, double Ig0 = 5e-11, double IDelta_omega = 0.0, double iAlpha = 0.0) // 2.3e-5
//	: n_fg(In_fg), Gamma_B(IGamma_B), g0(Ig0), Delta_omega(IDelta_omega), rho0(0.0), alpha(iAlpha) {
//	;
//};
//const double n_fg, Gamma_B, g0, Delta_omega;
//const double rho0;
//const double alpha;


//	Config(double iN = 1e3, double iL = 50.0, double iMaxErr = 1e-5, double iMaxIteration = 10)
//: L(iL), N(iN), h(L / N), max_err(iMaxErr), max_iteration(iMaxIteration) {
//	;
